# 安装MkDocs
```
pip install mkdocs
```
# Fork此库

# clone自己复制过去的库
```
git clone 刚刚那个库
```
# 网站部署
```
mkdocs build
mkdocs gh-deploy
```
# 提交修改
```
git add .
git commit -m "web"
git push origin main:main
```